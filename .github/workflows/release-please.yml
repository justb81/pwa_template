# SPDX-FileCopyrightText: 2026 Bastian Rang and contributors
# SPDX-License-Identifier: Apache-2.0
name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

# Never cancel a release run midway — a half-created release/tag is worse than a
# queued one.
concurrency:
  group: release-please-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  HUSKY: '0'

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release-please.outputs.release_created }}
    steps:
      - uses: googleapis/release-please-action@v5
        id: release-please
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          # A PAT is optional. It is only needed so that CI also runs on the
          # release PR itself: pushes made with the default GITHUB_TOKEN don't
          # trigger other workflows (GitHub's anti-recursion guard). The Pages
          # deployment no longer depends on it — deploy.yml is called from this
          # workflow rather than by a `release: published` event.
          token: ${{ secrets.RELEASE_PLEASE_TOKEN || secrets.GITHUB_TOKEN }}

      # release-please writes CHANGELOG.md and package.json with its own
      # formatting, which does not match our Prettier config — `pnpm format:check`
      # would fail on the release PR. Reformat and push back onto its branch.
      - name: Check out the release PR branch
        if: ${{ steps.release-please.outputs.pr }}
        uses: actions/checkout@v7
        with:
          ref: ${{ fromJSON(steps.release-please.outputs.pr).headBranchName }}
          token: ${{ secrets.RELEASE_PLEASE_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Install pnpm
        if: ${{ steps.release-please.outputs.pr }}
        uses: pnpm/action-setup@v6

      - name: Set up Node
        if: ${{ steps.release-please.outputs.pr }}
        uses: actions/setup-node@v7.0.0
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        if: ${{ steps.release-please.outputs.pr }}
        run: pnpm install --frozen-lockfile

      - name: Format release PR
        if: ${{ steps.release-please.outputs.pr }}
        run: |
          pnpm format
          if ! git diff --quiet; then
            git config user.name 'github-actions[bot]'
            git config user.email 'github-actions[bot]@users.noreply.github.com'
            git commit -am 'style: format release-please output'
            git push
          fi

  # Publishing to Pages happens here, not on `release: published` — see the
  # comment at the top of deploy.yml for why.
  deploy:
    needs: release-please
    if: ${{ needs.release-please.outputs.release_created == 'true' }}
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: ./.github/workflows/deploy.yml
